; Fold control structures
[
  (if_statement)
  (for_statement)
  (each_statement)
] @fold

; Fold components
(component) @fold

; Fold layout literals
(layout_literal) @fold

; Fold objects and arrays
[
  (object)
  (array)
] @fold

; Fold function calls
(function_call) @fold
